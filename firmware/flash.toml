# Flash configuration for ESP-IDF projects.
#
# Tip: set per-project ports if you have multiple dev boards attached.

[receiver]
port = "/dev/ttyACM0"
baud = 115200

[transmitter]
port = "/dev/ttyACM1"
baud = 115200
